# MURPHY-Inspired Code Generation Agent

Multi-turn self-correcting code generation system based on the
[MURPHY paper](https://arxiv.org/abs/2511.07833) (Multi-Turn GRPO
for Self-Correcting Code Generation).

## Setup
```bash
python -m venv venv
source venv/bin/activate        # Linux
venv\Scripts\activate           # Windows
pip install -r requirements.txt
